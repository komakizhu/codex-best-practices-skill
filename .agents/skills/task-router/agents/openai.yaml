interface:
  display_name: "Task Router"
  short_description: "Route repository work to native workflows"
  default_prompt: "Use $task-router to route this repository task to the lightest native workflow."
policy:
  allow_implicit_invocation: true
