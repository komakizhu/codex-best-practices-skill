interface:
  display_name: "Task Router"
  short_description: "Route a task from the engineering entry point"
  default_prompt: "Use $task-router to classify this repository task, require RCA before any Bug fix, show the route handoff, and wait for route confirmation before the next stage."
policy:
  allow_implicit_invocation: false
