interface:
  display_name: "Task Router"
  short_description: "Route a task from the engineering entry point"
  default_prompt: "Use $task-router as the lower-level router for this repository task."
policy:
  allow_implicit_invocation: true
