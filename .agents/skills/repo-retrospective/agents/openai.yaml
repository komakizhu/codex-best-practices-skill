interface:
  display_name: "Repository Retrospective"
  short_description: "Capture only repeatable repository friction"
  default_prompt: "Use $repo-retrospective after engineering work to check for small, evidence-backed repository improvements."
policy:
  allow_implicit_invocation: true
