interface:
  display_name: "Engineering Workflow"
  short_description: "Route repository work through native Codex capabilities"
  default_prompt: "Use $engineering-workflow to show the five-item brief, confirm it, and route this repository task through its required native stages."
policy:
  allow_implicit_invocation: false
