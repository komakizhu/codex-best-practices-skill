interface:
  display_name: "Engineering Workflow"
  short_description: "Route repository work through native Codex capabilities"
  default_prompt: "Use $engineering-workflow to distinguish exploration, symptom-only Bug reports, and explicit repair requests; send Bug reports through read-only $rca-analyze, then show/confirm brief, route, Option, native Plan, execution, and optional retrospective handoffs."
policy:
  allow_implicit_invocation: false
