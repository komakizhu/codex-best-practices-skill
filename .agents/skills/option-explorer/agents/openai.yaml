interface:
  display_name: "Option Explorer"
  short_description: "Compare high-cost engineering paths when no clear winner exists"
  default_prompt: "Use $option-explorer only if the task has multiple materially different high-cost options and the user opts in."
policy:
  allow_implicit_invocation: false
