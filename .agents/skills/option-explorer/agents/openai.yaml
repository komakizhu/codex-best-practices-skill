interface:
  display_name: "Option Explorer"
  short_description: "Compare high-cost engineering paths when no clear winner exists"
  default_prompt: "Use $option-explorer only after showing the trigger and receiving 进入 option; after exploration, wait for a方案选择 before Plan or implementation."
policy:
  allow_implicit_invocation: false
