interface:
  display_name: "Task Brief"
  short_description: "Turn scattered repository requests into concise task definitions"
  default_prompt: "Use $task-brief to produce the five-item task brief before routing this repository task."
policy:
  allow_implicit_invocation: false
